version: 2

sources:
  - name: raw
    schema: DBT_STUNGARIA    # CHANGE THIS to your actual schema name
    tables:
      - name: raw_customers
      - name: raw_orders
      - name: raw_products
      - name: raw_payments